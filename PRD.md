# 수업 포털 웹앱 PRD

장평중학교 1학년 정보과 · 2026학년도 2학기
최초 작성 2026-08-10 · **현행화 2026-08-11** (1차시 운영일)

이 문서는 설계 의도와 **현재 구현 상태**를 함께 담는다. 구현이 원안과 달라진 곳은 그
이유를 함께 적었다. 설계 근거를 지우고 결과만 남기면, 다음에 같은 고민을 처음부터
다시 하게 된다.

**구현 상태 범례** — ✅ 구현 완료 · ◐ 부분 구현 · ○ 미구현

---

## 0. 구현 현황 요약

| 영역 | 상태 | 비고 |
|---|---|---|
| 진입 · 인증 (코드 → 학번 → 확인) | ✅ | 코드 자동 만료는 시각표 등록 기간에만 (3.1) |
| 교사가 제어하는 수업 단계 | ✅ | 원안의 "슬라이드 URL 임베드"를 대체 (3.2) |
| 대기 중 미니게임 + 원리 설명 팝업 | ✅ | 원안에 없던 추가 (3.2) |
| 감정 체크 (무드미터 16종) | ◐ | 저장·집계는 동작. **사분면 배치가 안내문과 반대** (D1) |
| 한 줄 성찰 → **질문별 답변** | ✅ | 자동 임시저장 포함 (3.4) |
| 영상 — 전자칠판 전용 재생 | ✅ | 학생 태블릿에 영상 URL을 안 내려보냄 |
| 출석 (접속 로그) | ✅ | 인증 완료 시각으로 확정 |
| 수업 내용 작성 (교사) | ◐ | 제목·본문·URL만. **카드/탭은 UI에서 못 만듦** (3.2) |
| 교사 대시보드 · 명렬표 · 차시 · 시간표 | ✅ | 읽기량 문제 있음 (D2) |
| 성찰 모아보기 | ◐ | 선택한 차시 안에서만. 학생별 누적 열람 없음 |
| CSV 내보내기 · 일괄 삭제 | ✅ | 수동 실행 · 확인 문구 입력 방식 |
| 학생 본인 누적 기록 | ✅ | `/lesson/history` |
| 감정 시각화 형태 전환 (나침반형·분포형) | ◐ | 무드미터 격자 1종만 |
| 학생용 지난 수업 다시 보기(아카이브) | ○ | 본인 기록 열람과는 다른 기능 |
| 산출물 담벼락 | ○ | P2 · 9월 이후 |
| Gemini API 연동 | ○ | 6.5의 전제 조건 미확인. **안내 문구는 이미 노출 중** (5.3) |

> **미해결 결함 4건**(D1~D4)이 있다. 10장에 따로 모아 두었다.
> 이 문서는 "그렇게 되어야 한다"가 아니라 **지금 코드가 실제로 그렇다**를 적은 것이다.

---

## 1. 배경과 목표

2학기부터 모든 수업 슬라이드를 웹앱으로 제작한다. 학생이 매번 다른 링크를 찾아다니지 않도록,
**하나의 대문(포털)에 들어오면 오늘 자기 반 수업으로 바로 이어지는** 구조를 만든다.

포털은 네 가지를 겸한다.

1. **수업 진입점** — 오늘 수업 화면으로 연결
2. **활동 기록 누적** — 감정 체크, 성찰이 학기 내내 쌓임
3. **출석 확인** — 접속 기록으로 대체
4. **학생 산출물 공유** — 결과물을 올리고 서로 보는 담벼락 (○ 미구현)

### 대체 대상

| 기존 도구 | 대체하는 기능 | 도입 시기 | 상태 |
|---|---|---|---|
| 노션 | 대문, 수업 자료·링크 아카이브, 지난 수업 다시 보기 | 8~9월 | ◐ 대문만 |
| 패들렛 | 학생 산출물 게시·공유 담벼락 | 9월 이후 | ○ |
| (신규) | 수업 화면 구동 | 8월~ | ✅ |
| (신규) | 교사용 대시보드 — 출석·감정·성찰·진행 상황 | 8월~ | ✅ |

도구를 하나로 모으는 실질적 이유: 30분 수업에서 도구를 전환할 때마다 접속·설명에 2~3분이 사라진다.
로그인 체계가 하나면 그 손실이 없다.

### 설계 제약 (중요)

| 제약 | 영향 | 구현에 반영된 곳 |
|---|---|---|
| 실질 수업 시간 **30분** (40분 단축수업 − 태블릿 수령 2분 · 반납 5분, 부팅 지연 포함) | 진입 동선은 **1분 내** | 숫자 키패드, 자동 세션 선택, 웹폰트 미사용 |
| 학생 태블릿 **QR 인식 불가** | 링크 직접 배포 | — |
| 학생 28명 **동시 접속** (한 번에 한 반) | 초기 로딩 최소화 | 웹폰트 없음, 폴링 간격 4~5초 |
| 중학교 1학년 | 입력은 숫자 위주, 화면당 할 일 하나 | 교사 제어 단계(3.2), 본문 18px |

### 규모

- 1학년 **1~4반**, 반당 약 28명, 총 약 **112명**
- 반당 주 3차시 × 4반 = **주 12차시**, 주 5일 기준 하루 2~3차시

**Firestore 읽기량 — 재산정 필요 (D2)**

최초 산정은 "하루 쓰기 250건 / 읽기 1,500건 → 무료 한도의 1~3%"였다. 이는 학생 측 요청만 센
값이고, **교사 대시보드 폴링이 들어간 지금 구현과는 맞지 않는다.**

대시보드는 5초마다 호출되고, 한 번마다 출석·감정·성찰·명렬표를 전부 다시 읽는다.

| 항목 | 추정치 |
|---|---|
| 폴링 1회 | **115~116건** = 오늘 세션 목록(2~3) + 대상 세션(1) + 28명 × 4종(출석·감정·성찰·명렬표) |
| 30분 수업 1회 | 360회 × 115 ≈ **41,000건** |
| 하루 2~3차시 | **83,000 ~ 124,000건** |
| Spark 무료 한도 | **50,000건/일** |

이 수치는 **상한**이다. 28명의 감정·성찰이 수업 시작부터 모두 채워져 있다고 가정했고, 실제로는
수업이 진행되면서 늘어난다. 반대로 이것은 **대시보드 한 화면만** 센 값이라, 교실 앞 공유 화면
(`/teacher/board`)까지 함께 띄우면 프로젝트 전체 읽기량은 더 커진다.

**하루 한 차시만 대시보드를 열어 둬도 한도의 80%대에 이른다.** 한도를 넘으면 수업 도중 읽기가
거부된다. 대책은 하나를 고르는 문제가 아니라 **여러 개를 겹쳐 쓰고 실제 사용량을 측정해야 하는
문제다** (10장 D2).

### 인프라 · 기술 스택 (확정 · 구현 반영)

- **Next.js 16 (App Router) · React 19 · Tailwind CSS 4**
- **Firebase Firestore** — 리전 `asia-northeast3`(서울), Spark(무료) 플랜
- **Vercel** 배포
- Google Cloud **예산 알림** 설정 (이상 징후 조기 감지)
- 무료 티어에 자동 백업이 없으므로 **주 1회 CSV 내보내기**를 운영 습관으로

**학생 브라우저는 Firestore에 직접 접근하지 않는다.** 학생 화면의 모든 읽기·쓰기는 Next.js
Route Handler(서버)를 거치고, 서버만 Firebase Admin SDK를 쓴다. 그래서 `firestore.rules`를
전면 차단으로 둘 수 있고, **명렬표가 학생 클라이언트로 내려가는 경로가 아예 존재하지 않는다.**
이것은 성능 선택이 아니라 개인정보 설계다.

(교사 화면은 다르다. `/api/teacher/students`는 명렬표 전체를 교사 브라우저로 내려보낸다.
명렬표 관리 화면에 필요한 데이터이고, 교사 세션이 있어야만 도달한다.)

**교사 ID 토큰 검증은 Identity Toolkit REST로 한다.** `firebase-admin/auth`를 Vercel 함수에서
쓰면 함수가 죽는 문제가 있었다. 검증에 필요한 것은 토큰 확인 한 가지뿐이라 REST 호출로 대체했다.

---

## 2. 사용자

- **학생** — 1학년. 태블릿 브라우저로 접속. 계정·비밀번호 없음.
- **교사** — 수업 진행자. 대시보드 열람, 명렬표·수업·코드 관리.

**교사 인증 (확정 · 원안에서 강화)**
원안은 "교사만 아는 URL + 비밀번호 정도"였으나, 실제로는 **Firebase Google 로그인 +
`TEACHER_EMAILS` 허용목록**으로 구현했다. 비밀번호를 앱이 직접 들고 있지 않아도 되고,
허용목록에 없는 계정은 로그인에 성공해도 통과하지 못한다.

`TEACHER_EMAILS`가 비어 있으면 **아무도** 교사 화면에 들어올 수 없다. 열어두는 쪽이 더
위험하므로 의도한 기본값이다.

---

## 3. 핵심 동선

### 3.1 진입 · 인증 (P0) — ✅ 구현

순서가 중요하다. **수업 코드를 먼저 받는다.**

```
① 수업 코드 (숫자 2자리)          POST /api/student/code
     ↓  코드 유효성 + 오늘 그 교시 수업 매칭 → 단기 코드 토큰 발급
② 학번 (숫자 5자리, 예: 10209)     POST /api/student/identify
     ↓  명렬표 대조 + 코드에 묶인 반과 학번 앞 3자리 일치 검사
③ 확인 팝업 "1학년 2반 9번 ○○○ 학생 맞나요?"
     [네, 맞아요]  [아니에요 → ②로 복귀]   POST /api/student/confirm
     ↓  여기서 비로소 학생 세션 발급 + 출석 기록 + 수업 시작 처리
④ 오늘 수업 화면 (/lesson)
```

**코드를 먼저 받는 이유**: 학번이 규칙적(`10209`)이라 학번을 먼저 받으면 아무 숫자나 넣어
이름을 조회할 수 있다. 코드가 앞에 있으면 그 교실에 있는 학생만 이름 확인 화면에 도달한다.

**단계마다 토큰을 분리한다.** ①을 통과하면 **코드 토큰**(10분)만 나온다. 학번 조회는 이 토큰이
있어야 가능하다 — 명렬표 열람의 1차 방어선이다. 학생 세션은 ③ 확인 이후에만 발급된다.

**세션 상태는 ①에서 바꾸지 않는다.** 코드를 찍어 맞힌 것만으로(아직 학생인지도 모른다) 수업이
시작 상태가 되면 차시 스냅샷이 그 시점에 고정된다. 실제 시작 처리는 ③에서 한다.

**반 불일치 차단**: 수업 코드는 `(날짜, 교시, 반)`에 묶인다. 3교시 코드가 1학년 2반 수업이면
학번 앞 3자리가 `102`가 아닌 학생은 통과하지 못한다. 다른 반 코드를 알아내도 소용없다.

**같은 반 친구 학번 도용**: 앱에서 막지 않는다. 교사가 수업 중 출석 확인으로 통제한다.
(교사 대시보드에 접속자 명단을 5초마다 갱신 표시)

**학번 범위 규칙**

| 번호 | 취급 |
|---|---|
| `01`~`30` | 정상 (정원 28명 + 전입 여유 2) |
| `31`~`89` | **거부** — 오타로 본다 |
| `90`~`99` | 임시 번호 — 이름 없이 통과 |

명렬표에 없는 학생(전입·오류)은 그 반 90번대로 진입한다(예: `10291`). 수업 흐름을 끊지 않는 것이
목적이고, 실제 학번 연결은 교사가 나중에 `/teacher/students`에서 한다.

**수업 코드 자동 만료 (구현 추가)**
교사가 종료 버튼을 누르는 것에만 의존하면, 깜빡한 날의 코드가 하교 후까지 살아 있다.
`src/lib/timetable.ts`에 교시 시각표를 두고 **교시 종료 + 10분 유예** 후 자동으로 닫는다.

- 유예 10분: 종이 울린 뒤 마무리하는 일이 흔하고, 늦게 들어온 학생이 성찰을 못 남기면 그날
  기록이 통째로 빈다. 코드를 조금 늦게 닫는 쪽의 손해가 훨씬 작다.
- 시각표를 모르는 날짜는 자동 만료를 **적용하지 않는다.** 근거 없이 코드를 닫아 학생을 못
  들어오게 만드는 쪽이 훨씬 나쁘다.
- `identify`와 `confirm`에서 **다시 검사한다.** 코드 토큰은 10분 살아 있으므로 그 사이 교시가
  끝날 수 있고, `status`만 보면 자동 만료를 통과해 버린다.

> **현재 등록된 시각표는 `2026-08-10 ~ 2026-08-21` 단축 시간표 하나뿐이다 (D3).**
> 위의 "모르는 날짜는 적용하지 않는다" 규칙 때문에, **8월 22일부터는 자동 만료가 조용히
> 꺼진다.** 오류도 경고도 나지 않고 그냥 동작하지 않는다. 정상 시간표 시작 전에 반드시
> `src/lib/timetable.ts`에 다음 기간을 추가해야 한다.

**자동 만료는 `status`를 바꾸지 않는다.** 닫혔는지는 조회할 때마다 계산한다(`isSessionClosed`).
따라서 교사가 종료를 누르지 않은 세션은 시간이 지나도 `status`가 `active`로 남는다 — 이것이
4.3의 세션 자동 선택에 영향을 준다 (D4).

**코드 중복 발급 방지**: `codeReservations` 문서 ID를 `날짜__코드`로 두어 Firestore가 뒤엣것을
거부하게 한다. 조회 후 배정하는 방식이면 같은 날 두 반에 같은 코드가 나갈 수 있고, 그러면 한쪽 반
학생 전원이 반 불일치로 막힌다.

**속도 제한 (구현 추가)**: 기기 단위(코드 12회/분, 학번 15회/분)와 네트워크 단위(600회/분)를
함께 건다. **교실 전체가 한 IP로 나가므로 IP로만 세면 옆자리 학생까지 막힌다.**

단, 이것은 **인스턴스 메모리에 카운터를 두는 최선 노력형 제한**이다. 서버리스에서는 함수
인스턴스마다 카운터가 따로 놀고, 기기 쿠키를 지우면 초기화된다. 학번을 훑어 이름을 모으는
행동을 **늦추는** 용도이지 막는 장치가 아니다. **진짜 방어선은 "코드를 맞힌 사람만 학번 화면에
도달한다"는 동선 자체다.**

**세션 유지**: 한 차시 동안 재입력이 없도록 서명된 HttpOnly 쿠키로 유지. 만료는 당일 자정.

### 3.2 수업 화면 (P1) — ✅ 구현 · **원안에서 변경**

원안: 외부 슬라이드 웹앱의 URL을 세션에 보관하고 임베드/이동한다.
현재: **포털이 수업 내용을 직접 그리고, 지금 어느 단계인지는 교사가 정한다.**

**왜 바꿨나.** 외부 슬라이드를 임베드하면 학생 30명이 각자 다른 지점에 가 있게 된다. 앞으로
넘긴 학생, 뒤에 남은 학생, 유튜브로 빠져나간 학생이 동시에 생기면 교사가 수업을 끌고 갈 수 없다.
중1은 화면당 할 일이 하나여야 한다는 제약과도 정면으로 부딪힌다.

**학생에게 화면 이동 권한을 주지 않는다.** 학생 화면은 4초마다 `/api/student/phase`를 확인해
교사가 정한 단계를 따라간다.

**수업 단계 (`LessonPhase`)**

| 단계 | 학생 화면 |
|---|---|
| `waiting` 대기 | 미니게임 (있으면) / 대기 안내 |
| `mood` 기분 | 무드미터 |
| `progress` 진도 안내 | **카드** — 단원을 나란히 놓고 비교 |
| `assessment` 평가 안내 | **탭** — 수행평가를 번갈아 확인 |
| `video` 영상 시청 | "앞 화면을 보세요" 안내 + 생각할 질문 |
| `reflection` 성찰 | 질문별 입력칸 |
| `done` 마침 | 정리 안내 |

진도는 카드, 수행평가는 탭이다. 형식이 다른 이유는 읽는 방식이 다르기 때문이다 — 진도는 여러 개를
한눈에 훑고, 수행평가는 하나씩 파고든다. 긴 문단은 중1이 한눈에 읽지 못하므로 `cards`/`tabs`가
있으면 그것으로 그리고, 없을 때만 본문을 그대로 보여준다.

> **제약 — 카드·탭은 교사 화면에서 만들 수 없다 (◐).**
> 타입·API·학생 화면 렌더링은 `cards`/`tabs`를 모두 지원하지만, 차시 편집기
> (`/teacher/lessons`)의 입력칸은 **제목·본문·URL 세 개뿐이다.** 따라서 지금 교사가 화면에서
> 새로 만들 수 있는 것은 본문 형식까지이고, 카드·탭 구조는 편집기에 입력 UI가 붙기 전까지
> 화면에서 신규 작성·수정할 수 없다.

**영상은 전자칠판에서만 재생한다 (구현 추가).**
학생 태블릿에는 **영상 URL 자체를 내려보내지 않는다.** 화면 코드가 실수해도 학생 기기에서
영상이 열릴 수 없다. 재생은 교사용 `/teacher/screen`에서만 한다. 학생 화면에는 시청 안내와
"영상을 보면서 생각할 것"(성찰 질문)만 크게 띄운다.

**대기 시간 미니게임 + 원리 설명 팝업 (구현 추가 — 원안에 없음)**

태블릿 부팅과 주소 오타 때문에 학생마다 도착 시각이 5분씩 벌어진다. 먼저 온 학생의 그 시간을
그냥 버리지 않으려고 대기 단계에 게임을 띄운다. 수업이 시작되면 화면이 저절로 넘어가므로 학생이
게임을 끄고 나올 필요가 없다.

다만 **게임으로만 끝나면 남는 게 없다.** 대기에서 수업으로 넘어가는 순간, "방금 그 게임의 원리"
팝업을 **한 번만** 띄운다. 놀이를 수업 내용에 붙이는 고리다.

**지난 수업 다시 보기** — 학생용 아카이브는 아직 없다(○). 본인이 쓴 기록 열람(`/lesson/history`)은
있으나 이것은 다른 기능이다.

### 3.3 감정 체크 (P1) — ◐ 부분 구현

매 차시 도입에 수행. 사회정서교육 연계. 차시별로 켜고 끌 수 있다(`moodCheckEnabled`).

**감정 선택지 (확정)** — 무드미터 4사분면 × 4개 = **16개**.
축은 `valence`(불쾌 −2 ~ 쾌 +2) × `arousal`(비활성 −2 ~ 활성 +2).
사분면당 4개로 제한한 것은 중1이 3초 안에 고르게 하기 위해서다.

| 사분면 | 뜻 |
|---|---|
| 빨강 | 기운은 높은데 기분은 나쁨 |
| 노랑 | 기운도 높고 기분도 좋음 |
| 파랑 | 기운도 낮고 기분도 나쁨 |
| 초록 | 기운은 낮지만 기분은 좋음 |

사분면 색은 장식이 아니라 축을 읽는 단서다. 임의로 바꾸면 무드미터가 아니게 된다.

> **결함 D1 — 화면 배치가 안내 문구와 반대다.**
> 학생 화면은 "위로 갈수록 기운이 높고, 오른쪽으로 갈수록 기분이 좋아요"라고 안내하지만,
> 실제 배치는 **가로축이 기운, 세로축이 기분(위가 불쾌)** 이다. 두 축이 서로 뒤바뀌어 있다.
>
> | 현재 배치 | 안내문대로라면 |
> |---|---|
> | `[파랑][빨강]` ← 불쾌 | `[빨강][노랑]` ← 기운 높음 |
> | `[초록][노랑]` ← 쾌 | `[파랑][초록]` ← 기운 낮음 |
>
> 교실 앞 공유 화면(`/teacher/board`)도 같은 순서를 쓴다.
>
> **데이터는 무사하다.** 학생이 누르는 것은 위치가 아니라 감정어 버튼이고(화남·뿌듯함…),
> 저장되는 `valence`/`arousal`은 그 감정어에 붙어 있는 값이다. 따라서 **기록 자체는 정확하고,
> 배치를 고치면 지금까지 쌓인 데이터도 그대로 정상 시각화된다.**
>
> 문제는 학생이 겪는 혼란이다. 안내문을 따라 "기운이 높으니 위쪽"을 보면 엉뚱한 사분면에
> 도착하고, 색으로 축을 읽으라는 설계 의도도 무너진다. 10장 D1 참조.

**공개 범위 — 확정**

| | 학생에게 | 교사에게 |
|---|---|---|
| 감정(아이콘·색) | 익명 집계로 공개 | 개인별 열람 |
| 이유 한 줄 | **비공개** | 열람 (+ AI 요약, 6.5 참조) |
| 자기 기록 | 본인 것만 열람 | — |

**공유 화면과 개별 열람은 API부터 분리했다.**
교실 앞에 띄우는 `/teacher/board`가 쓰는 `/api/teacher/board` 응답에는 **학번·이름·이유가 아예
들어 있지 않다.** 집계 수치와 사분면 분포만 내려간다. 화면 코드가 실수해도 개인 응답이 뜰 수 없다.
개별 열람은 `/api/teacher/dashboard`로 완전히 분리되어 있다.

**시각화** — 지금은 무드미터 격자 1종(◐). 나침반형·분포형 전환은 9월 과제.

**출석과의 분리**: 출석은 ③ 인증 완료 시각(접속 로그)으로 확정한다. 감정 응답 여부는 출석 요건이
아니다. 감정을 출석 근거로 삼으면 학생이 아무거나 찍고 넘어가는 것으로 학습한다.

**미확인 추적**: 각 응답에 `reviewedByTeacher`를 두고, 대시보드가 미확인 건수를 항상 보여준다.
확인 처리는 지금 **"모두 확인" 일괄 버튼 하나뿐이다.** API는 학생을 지정한 확인도 받을 수 있으나
화면에는 버튼이 없다 (5.4 참조).

### 3.4 성찰 (P0) — ✅ 구현 · **원안에서 변경**

원안: "한 줄 성찰" — 차시당 질문 1개, 답 1개.
현재: **차시당 질문 여러 개, 질문별로 따로 답한다.**

**왜 바꿨나.** 1차시 성찰 질문이 3개였는데, 입력칸 하나에 세 답을 몰아 쓰게 하면 학생은 대개
첫 질문에만 답한다. 나중에 읽을 때 어느 답이 어느 질문에 대한 것인지도 알 수 없다.
`reflections.answers[]`의 인덱스가 세션 스냅샷 `reflectionQuestions`의 순서와 짝을 이룬다.

- 차시마다 **교사가 지정한 질문**을 띄운다.
- 학생은 자기 글의 **누적 기록**을 볼 수 있다 (`/lesson/history` — 질문과 답을 짝지어 보여준다).
- 다른 학생 글의 공개 여부는 **차시별 교사 설정**(`reflectionPublic`)으로 켜고 끈다. 기본값 비공개.
- 30분 수업의 마지막 활동이므로 **미완성 상태로 종료되어도 저장**된다 — 입력이 멈춘 뒤 1.5초에
  자동 임시저장(`draft: true`), 제출 시 `draft: false`.
- 제출 직후 늦게 도착한 자동저장이 제출 상태를 초안으로 되돌리지 않도록, 제출 시 예약된
  자동저장을 먼저 취소한다.
- 답이 몇 개 중 몇 개 채워졌는지를 화면에 표시한다 (`2/3 작성`).

**옛 문서 방어**: 질문이 하나였던 시절 문서에는 `answers`가 없다. 그런 문서 하나 때문에 교사
대시보드 전체가 500으로 죽으면 수업 중에 손쓸 방법이 없다. 읽는 쪽에서 항상 배열을 보장한다
(`answersOf()`).

### 3.5 산출물 담벼락 (P2 — 9월 이후) — ○ 미구현

패들렛을 대체하는 부분. 학생이 결과물을 올리고 서로 본다.

**올릴 수 있는 것**: 텍스트 · 이미지 · 링크. 차시별 담벼락 단위.

**공개 범위**: 같은 반, 같은 차시 안에서만. 다른 반 담벼락은 보이지 않는다.

**상호작용 — 최소한만 연다**

| 기능 | 채택 | 이유 |
|---|---|---|
| 반응(이모지 1~2종) | ○ | 참여감은 주되 서열화는 피함 |
| 댓글 | **×** | 사이버 폭력 관리 부담이 급증 |
| 익명 게시 | **×** | 실명(학번+이름) 표시가 안전장치 |
| 교사 즉시 숨김 | ○ | 부적절 게시물 1클릭 처리 |

패들렛에서 흔히 생기는 문제(장난 게시물, 서로 저격, 익명 뒤에 숨기)를 **기능을 안 만드는 방식으로**
막는다. 9월에 사이버 폭력을 가르치는 수업에서, 플랫폼 자체가 그 사고를 만들면 곤란하다.

**이미지 업로드 주의 — Blaze 필요**

Firebase Cloud Storage는 2026년 2월부터 Spark 플랜에서 사용할 수 없다. 이미지 업로드를 넣는 순간
**Blaze 전환이 필요**하다.
- 사용량 자체는 무료 한도 안에 들어갈 가능성이 높다
- 다만 예산 알림은 반드시 설정한다
- 이미지 없이 텍스트·링크만으로 시작하면 Spark 유지 가능

**용량 관리**: 112명 × 이미지가 학기 내내 쌓이면 무시할 수 없다. 업로드 시 리사이즈, 학기말 일괄
삭제 정책 적용.

---

## 4. 교사 화면

### 4.1 화면 목록 (구현됨)

| 경로 | 화면 | 용도 |
|---|---|---|
| `/teacher` | 로그인 | Google 계정 + 허용목록 |
| `/teacher/dashboard` | 대시보드 | 수업 코드, 접속자 명단, 단계 제어, 감정·성찰 열람, 메모 |
| `/teacher/board` | 공유 화면 | 교실 앞 **익명** 감정 집계 |
| `/teacher/screen` | 전자칠판 | 영상 재생 전용 |
| `/teacher/lessons` | 차시 | 차시 계획 등록·수정, 수업 하나만 바로 열기 |
| `/teacher/schedule` | 시간표 | 반별 요일·교시 → 학기 전체 세션 생성 |
| `/teacher/students` | 명렬표 | CSV 업로드, 임시번호 연결, 삭제 |
| `/teacher/data` | 데이터 | CSV 내보내기, 일괄 삭제 |

### 4.2 기능별 상태

| 기능 | 우선순위 | 상태 | 비고 |
|---|---|---|---|
| 오늘 접속자 실시간 명단 | P0 | ✅ | 5초 폴링, 출석 확인 겸용 |
| **수업 단계 제어** | P0 | ✅ | 원안에 없던 항목 (3.2) |
| 감정 대시보드 (공유용, 익명) | P1 | ◐ | API 분리는 ✅. **격자 배치는 D1의 영향을 같이 받는다** |
| 감정 개별 응답 열람 | P1 | ✅ | 공유용과 화면 분리 |
| 감정 미확인 추적 | P1 | ✅ | 5.4 |
| 감정 확인 처리 | P1 | ◐ | **일괄만.** 개별 확인은 API만 있고 UI 없음 |
| 성찰 글 모아보기 | P0 | ◐ | **선택한 차시 안에서만.** 학생별 누적 열람은 없음 |
| 명렬표 관리 | P0 | ✅ | CSV 업로드, 임시번호 연결 |
| 차시 등록 (`lessonPlans`) | P0 | ✅ | 4반 공용 |
| 시간표 입력 → 세션 자동 생성 | P0 | ✅ | |
| 반 전환 | P0 | ✅ | 날짜 + 세션 선택 |
| 수업 후 교사 메모 | P1 | ✅ | |
| 수업 코드 발급·조회 | P0 | ✅ | 자동 생성 + 대시보드에 크게 표시 |
| 데이터 내보내기 | P2 | ✅ | CSV (BOM 포함) |
| 데이터 일괄 삭제 | P1 | ✅ | 5.2 · 수동 실행 |
| 감정 시각화 형태 전환 | P1 | ◐ | 격자 1종 |

### 4.3 "지금 하는 수업"을 자동으로 고른다 (구현 추가) — ◐

교사 화면을 열면 오늘 첫 수업이 기본으로 잡히는 것이 자연스러워 보이지만, 그러면 3교시에
2교시 반의 데이터가 교실 앞 화면에 뜬다. **다른 반 학생의 기분이 노출되는 셈이라 그냥 불편한
문제가 아니다.**

선택 순서: ① 교사가 시작한(`active`) 세션 → ② 시각표상 지금 교시 → ③ 아직 안 끝난 첫 수업
→ ④ 첫 수업. 대시보드·전자칠판·공유 화면이 같은 규칙을 쓴다.

> **결함 D4 — 앞 교시를 종료하지 않으면 이 보호가 무력화된다.**
> ①이 최우선인데, 자동 만료는 `status`를 `ended`로 바꾸지 않는다(3.1). 그래서 교사가 2교시
> 종료 버튼을 누르지 않으면 3교시에도 **2교시 세션이 여전히 `active`라서 그대로 선택된다.**
> 막으려던 바로 그 상황이 발생한다. 지금은 **교사가 매 수업 끝에 종료를 누르는 것이 운영
> 전제**다. 10장 D4 참조.

### 4.4 폴링을 쓰는 이유 — 그리고 그 대가

Firestore 실시간 구독 대신 4~5초 폴링을 쓴다. 학생 브라우저가 Firestore에 직접 붙지 않는 구조를
유지하려면 교사 화면도 서버 API를 거쳐야 하기 때문이다.

**대가**: 대시보드 폴링 1회마다 출석·감정·성찰·명렬표를 전부 다시 읽는다. 28명 규모에서 응답
속도는 충분하지만 **읽기량이 무료 한도를 위협한다** (1장 · 10장 D2). 학생 화면의 단계 확인
(`/api/student/phase`)은 세션 문서를 캐시해서 읽으므로 이 문제에서 비교적 자유롭다.

---

## 5. 데이터

### 5.1 모델 (구현 반영)

```
students          문서ID = 학번 · 이름 · 반 · 번호 · 임시여부 · 연결학번

lessonPlans       차시번호 · 제목 · 감정체크여부
                  · game · gameExplainer · progress · assessment · video  (각각 PhaseContent)
                  · reflectionQuestions[] · reflectionPublic
                    ↑ 수업 내용. 반과 무관. 한 번만 등록해 4반에 공용

classSessions     문서ID = 날짜__교시__반
                  · lessonPlanId · 반 · 날짜 · 교시 · 수업코드
                  · status(scheduled|active|ended) · phase(수업 단계)
                  · 스냅샷(위 lessonPlans 내용 전부) · teacherNote
                  · startedAt · endedAt

codeReservations  문서ID = 날짜__코드                  ← 코드 중복 발급 방지

attendance        학번 · sessionId · 반 · 날짜 · joinedAt        ← 출석
moodEntries       학번 · sessionId · mood · valence · arousal · reason · reviewedByTeacher
reflections       학번 · sessionId · answers[] · draft
meta/purgeLog     마지막 일괄 삭제 일자
```

**`PhaseContent`** = `{ heading, body, url, cards?, tabs? }`.
`cards`는 진도처럼 나란히 비교하는 내용, `tabs`는 수행평가처럼 번갈아 보는 내용이다 (3.2).

**`lessonPlans` / `classSessions` 분리 이유**

4반이 같은 진도를 나가므로, 내용을 4번 등록하는 구조는 피한다. 한 번 등록하면 4반 세션에 자동
적용된다.

**스냅샷이 필요한 이유 (중요)**

교사는 1반 수업을 해보고 부족한 부분을 **다음 반 수업 전에 고쳐서** 진행한다. 이때 `lessonPlans`만
수정하면 이미 끝난 1반 기록이 소급 변경되어, "1반은 어떤 질문에 답한 것인가"를 알 수 없게 된다.

→ 세션 생성 시점에 수업 내용을 **세션에 복사(스냅샷)**한다.
- 수정은 **아직 시작하지 않은(`scheduled`) 세션에만** 반영된다. 차시 저장 시 반영된 세션 수를
  화면에 알려 준다.
- 끝난 세션은 그날 실제로 쓴 내용을 그대로 보존한다
- 성찰 답변을 나중에 읽을 때 질문과 짝이 맞는다 (`answers[i]` ↔ `reflectionQuestions[i]`)

**문서 ID로 중복을 막는다.** 세션 ID가 `날짜__교시__반`, 코드 예약 ID가 `날짜__코드`라서 두 요청이
동시에 들어와도 Firestore가 뒤엣것을 거부한다.

**교사 메모**

세션마다 수업 직후 한 줄 회고를 남긴다 ("영상 2분 넘어가니 늘어짐", "성찰 질문 2번 어려워함").
다음 반 수업 전에 이 메모를 확인하는 것이 개선 루프의 출발점이다.

**원칙: 이름은 `students`에만 둔다.** 활동 기록에는 학번만 저장하고, 화면에 보여줄 때만 조인한다.
기록 데이터가 유출되어도 숫자만 남는다.

### 5.2 보관 · 삭제 정책 — ✅ 구현

처음부터 넣는다. 나중에 붙이기 훨씬 어렵다.

| 데이터 | 보관 | 삭제 | 확인 문구 |
|---|---|---|---|
| 명렬표(이름) | 학기 중 | 학기 종료 시 | `명렬표삭제` |
| 감정 — 이유 한 줄 | 최소 기간 | **월 1회 수동 삭제** 권장 | `이유삭제` |
| 감정 — 아이콘·색 | 학기 중 | 학기 종료 시 | `감정삭제` |
| 성찰 글 | 학기 중 | 학기 종료 시 (필요분은 사전 내보내기) | `성찰삭제` |
| 접속 로그 | 학기 중 | 학기 종료 시 | `출석삭제` |

`/teacher/data`에 일괄 삭제와 **마지막 삭제 일자**가 있다. 되돌릴 수 없으므로 **확인 문구를 정확히
입력해야** 실행된다. "이유 한 줄"만 지우는 항목이 따로 있어서 아이콘·색 집계는 남길 수 있다.

**전부 수동이다.** 자동 삭제 스케줄러나 cron 라우트는 없다. 감정 이유의 월 단위 삭제는 교사가
직접 실행해야 하는 **운영 습관**이지 시스템이 보장하는 것이 아니다. 마지막 삭제 일자 표시가
그 습관을 추적하는 유일한 장치다.

### 5.3 안내 문구 (상설 노출)

감정 입력칸 옆에 항상 표시.

> 기분은 다 같이 보고, 적은 이유는 선생님이 봅니다.
> 내용을 정리할 때 AI 도구를 사용합니다.
> 힘든 일이 있으면 담임 선생님이나 상담 선생님께 이야기해 주세요.

두 번째 줄은 감정 데이터를 AI로 분석하기로 한 결정에 따른 것이다. 학생에게 "선생님만 봅니다"라고
안내하고 실제로는 외부 API를 거치면 약속이 깨진다. 9월에 개인정보 단원을 가르치는 수업에서 이
불일치는 특히 치명적이다.

> **지금은 반대 방향으로 어긋나 있다.** 이 문구는 학생 화면에 이미 상설 노출되고 있으나
> **AI 연동은 아직 없다**(6.5). 하지 않는 일을 한다고 안내하는 셈이다. 약속을 어기는 쪽보다는
> 덜 위험하지만, 개인정보를 가르치는 과목에서 정확하지 않은 고지는 그 자체로 좋지 않다.
> AI를 붙일 때까지 이 줄을 빼두거나 "앞으로 사용할 예정"으로 바꾸는 편이 정확하다.

세 번째 줄이 중요하다. **이 앱은 상담 창구가 아니다.** 매일 여러 반의 감정을 받으면 걱정되는
문장은 언젠가 반드시 들어온다.

### 5.4 교사 확인 루틴 — ◐ 부분 구현

교사는 **매 수업 종료 후 그날 감정 응답을 확인한다.** 받아두고 보지 않는 상태가 가장 위험하다.
대시보드가 **미확인 건수**를 항상 보여주고, 확인 처리를 기록한다.

**확인 처리는 "모두 확인" 일괄 버튼 하나뿐이다.** 개별 확인은 API(`/api/teacher/mood-review`)가
학생 ID 목록을 받도록 되어 있으나 화면에 버튼이 없다. 일괄 버튼만 있으면 읽지 않고 눌러 넘기기
쉬우므로, 확인 여부 추적이라는 원래 목적이 약해진다.

---

## 6. 1차시 (8/11) 범위

30분, 첫 사용. **동작 보장이 최우선.**

| 구분 | 기능 | 결과 |
|---|---|---|
| **반드시 동작** | 진입·인증 / 성찰 | ✅ 구현 |
| 되면 좋음 | 감정 체크 | ◐ 동작하나 배치 결함 (D1) |
| 없어도 됨 | 학생 태블릿에서 수업 내용 보기 | ✅ 구현 (단계 제어) |
| 이번엔 제외 | 지난 수업 다시 보기, 시각화 전환 | ○ / ◐ |
| (제외했다가 넣음) | 데이터 내보내기, 일괄 삭제 | ✅ 구현 |

### 1차시 흐름 (30분)

| 시간 | 내용 | 단계 |
|---|---|---|
| 5분 | 포털 접속 · 인증 (먼저 온 학생은 미니게임) | `waiting` |
| — | 수업 시작 — 게임 원리 설명 팝업 | (자동) |
| 4분 | 도입 — 영상 (0:00~2:08, 7:25~8:46) · 전자칠판 | `video` |
| 5분 | 단원 지도 | `progress` |
| 8분 | 수행평가 안내 | `assessment` |
| 3분 | 규칙 | `assessment` |
| 5분 | 성찰 남기기 | `reflection` |

### 성찰 질문 (1차시)

1. 영상에서 AI가 아직 못 한다고 한 것 중 하나를 고르고, 왜 어려울지 내 생각 쓰기
2. 완전히 대체될 직업은 7% 미만이라는데, 그럼 나머지는 어떻게 되는 걸까?
3. "AI와 함께 일하는 능력"이란 구체적으로 뭘까?

### 교사 테스트용 학생

교사가 학생 화면을 직접 걸어보기 위해 각 반 **30번**을 테스트 계정으로 두었다
(`10130` 가길동 / `10230` 나길동 / `10330` 다길동 / `10430` 라길동).

30번을 쓰는 이유: 실제 학생은 1~28번이라 겹치지 않고, 임시 번호(90번대)와 달리 **정규 학생과
똑같은 경로**로 진입하므로 학생이 실제로 겪는 화면을 그대로 볼 수 있다.

**주의 — 출석 집계에 포함된다.** 명렬표에 있으므로 정원 수에 잡히고, 접속하지 않으면 "아직 안 들어온
학생"에 뜬다. 수업 중 숫자가 헷갈리면 `/teacher/students`에서 삭제하면 된다.

### 실패 대비

- 포털이 열리지 않을 경우의 **대체 경로**를 준비한다 (성찰은 종이로).
- 첫날 "선생님 안 돼요"가 5분 이상 이어지면 학기 내내 인상이 남는다.
- **반납 5분 알람**을 고정한다.

### 운영 후 기록

> 1차시 운영 결과와 개선점은 수업 후 여기에 적는다. (세션별 교사 메모는 대시보드에 남는다)

---

## 6.5 AI 기능 (Gemini API) — ○ 미구현

차시에 따라 AI 기능을 붙일 수 있도록 준비한다. 아직 코드에 들어가지 않았다.
**붙이기 전에 아래 전제 조건을 먼저 확인한다.**

### 필수 원칙

**① API 키를 클라이언트에 두지 않는다**
학생 태블릿 브라우저에 키가 들어가면 그대로 노출된다. 반드시 **서버 경유** 호출.
- Firebase Cloud Functions는 Spark 플랜에서 사실상 쓸 수 없다 (Cloud Run·Artifact Registry 의존)
- 이미 Vercel을 쓰고 있으므로 **Route Handler**를 프록시로 둔다 (학생 API가 이미 이 구조다)
- 키는 Vercel 환경변수에 보관

**② 감정 데이터를 AI에 보낼 경우 — 유료 티어 필수**

- **과금이 활성화된 Gemini API 키(유료 티어)만 사용한다.** 무료 티어는 제출 내용이 Google 제품
  개선에 사용될 수 있고 사람이 검토할 수 있다. 중학생 정서 정보에는 부적합하다.
- **비식별 전송**: 학번·이름을 제거하고 텍스트만 보낸다. 매핑이 필요하면 요청 내 임시 인덱스만 사용.
- **배치 전송**: 개별 응답을 실시간으로 보내지 않고 반·차시 단위로 묶는다. 비용도 절반이 된다.
- **용도 제한**: 경향 요약·분류까지만. **개별 학생에 대한 진단·라벨링은 금지**한다.
  AI는 교사 판단의 보조이지 판정자가 아니다.
- **위기 신호**: AI로 1차 플래그를 띄우더라도 **반드시 교사가 원문을 확인**한다. AI가 놓칠 수
  있다는 전제로 교사 확인 루틴(5.4)은 그대로 유지한다.

**학생 안내 문구는 이미 노출되고 있다** (5.3). 즉 **고지가 구현보다 앞서 나가 있는 상태**다.
AI를 붙일 때 문구를 새로 만들 필요는 없지만, 붙이지 않기로 한다면 문구를 걷어내야 한다.

**학교 절차 확인**: 학생 정서 정보를 외부 API로 전송하는 것이 학교 개인정보 처리 방침과 보호자
안내 범위에 포함되는지 담당자와 확인한다. — **미확인**

**③ 학생 입력은 템플릿 안에서만**
프롬프트 전문을 학생이 자유 작성하게 두지 않는다. 서버에서 고정 프롬프트 + 학생 입력값 조합.

**④ 호출 한도**
학생 1인 1차시당 호출 횟수 상한을 서버에서 강제한다 (예: 5회). 112명 × 무제한은 비용·속도 양쪽에서
위험하다. (진입 API에 쓰는 `rateLimit`을 그대로 재사용할 수 있다)

**⑤ 사용 로그를 남긴다**
학번 · sessionId · 요청 시각 · 사용한 기능. 수행평가1이 AI 활용을 허용하고 **출처 밝히기 태도**를
평가하므로, 이 로그가 평가 근거가 된다. 학생에게도 "AI 사용 기록이 남는다"고 미리 안내한다.

### 기능 후보 (교사용부터)

| 대상 | 기능 | 우선순위 |
|---|---|---|
| 교사 | 성찰 글 112건 요약 · 키워드 추출 | 높음 |
| 교사 | 반별 응답 경향 정리 | 중간 |
| 학생 | 리포트 초안에 대한 피드백 (수행평가1) | 중간 |
| 학생 | 엔트리 코드 설명 (코딩 단원) | 낮음 |

**교사용을 먼저 붙이는 것이 안전하다.** 사용자가 1명이라 비용·남용 위험이 없고, 성찰 글 112건을
매주 눈으로 읽는 부담을 바로 덜어준다.

---

## 7. 학기 전체 로드맵

| 시기 | 추가 기능 | 상태 |
|---|---|---|
| 8월 | 진입·인증 ✅ · 성찰 ✅ · 수업 단계 제어 ✅ · **감정 체크 ◐**(D1) | ◐ |
| 9월 | 감정 시각화 전환, 지난 수업 다시 보기, 수행평가 산출물 제출(담벼락) | ◐ / ○ / ○ |
| 10~11월 | 피지컬 컴퓨팅 프로젝트 기록 (과정 관찰용 로그) | ○ |
| 12월 | 데이터 내보내기, 학기말 일괄 삭제 | ✅ (앞당겨 구현) |

**다음 작업 후보 (우선순위 순)**

1. **D1 무드미터 배치 · D2 읽기량** — 새 기능보다 먼저. 10장 참조
2. **D3 시각표 연장** — 8/22 이전
3. **D4 세션 자동 선택** — 운영으로 우회 가능하나 사람에게 의존한다
4. 차시 편집기에 카드·탭 입력 UI (3.2)
5. 감정 시각화 형태 전환 — 나침반형·분포형 (9월)
6. Gemini 교사용 성찰 요약 — 6.5 전제 조건 확인 후
7. 산출물 담벼락 — 텍스트·링크만으로 시작하면 Spark 유지 가능
8. 학생용 지난 수업 다시 보기(아카이브)

**수행평가와의 연계**
- **수행평가1 디지털 시민 리포트** (8~9월) — 성찰 글 누적이 포트폴리오 근거가 된다. 출처 밝히기
  태도를 평가하므로, 제출 양식에 **출처 입력란**을 두는 것을 검토.
- **수행평가2 피지컬 컴퓨팅 프로젝트** (10~11월) — 문제 발견부터 오류 수정까지의 **과정**을
  평가하므로, 학생이 진행 상황을 차시별로 남기는 기록 기능이 필요.

---

## 8. 결정 사항

### 8.1 확정 (구현 반영됨)

| 항목 | 결정 |
|---|---|
| 수업 코드 | 자동 생성 2자리. `(날짜, 교시, 반)`에 묶임. 교시 종료 + 10분 후 자동 만료. 같은 날 중복 발급 금지, 다음 날 재사용 가능 |
| 인프라 | Firebase(Firestore) + Vercel, Spark 무료, 서울 리전. Next.js 16 / React 19 / Tailwind 4 |
| 교사 인증 | Google 로그인 + `TEACHER_EMAILS` 허용목록 (원안의 "URL + 비밀번호"에서 강화) |
| 감정 선택지 | 무드미터 4사분면 × 4 = 16개 |
| 성찰 공개 기본값 | 비공개. 차시별 `reflectionPublic` 토글로 켠다 |
| 성찰 구조 | 차시당 질문 여러 개, 질문별 답변 |
| 수업 화면 제어 | 학생 이동 불가. 교사가 단계를 넘기면 학생 화면이 따라온다 |
| 영상 재생 위치 | 전자칠판 전용. 학생 태블릿에는 URL을 내려보내지 않음 |
| 디자인 | Figma 디자인 시스템(흑백 + 파스텔 블록). 웹폰트 미사용, 라이트 고정 |

**디자인 결정의 이유 두 가지**
- **웹폰트를 얹지 않는다** — 한글 웹폰트는 28명 동시 접속에서 첫 화면을 그만큼 늦춘다.
  시스템 한글 폰트를 쓰되 크기·굵기·자간 체계만 따른다. 본문 기본 18px.
- **라이트 모드로 고정한다** — 교실 태블릿이 다크로 잡히면 파스텔 색 블록이 무너진다.
  OS 설정만으로 다크가 켜지지 않도록 variant를 클래스 기반으로 바꿨다.

### 8.2 아직 열려 있음

1. **한 반의 학기 총 차시 수** — 시간표 자동 생성 범위. 반당 24차시면 세션 96개, 50차시면 약 200개.
2. **공동 지도 교사** — 교사 계정이 둘 이상 필요한지, 반을 나눠 보는지.
   (현재 `TEACHER_EMAILS`에 여러 개를 넣으면 모두 전체를 본다)
3. **학교망 접근 확인** — Firebase·Vercel 도메인이 학생 태블릿에서 열리는지.
4. **AI 기능 학교 절차 확인** — 6.5 참조. AI를 붙이기 전 필수.
5. **성찰 공개형 전환 시점** — 언제 다른 학생 글을 열어줄지.

---

## 9. 하지 않을 것

- 학생 비밀번호 자체 저장 (개인정보 처리 부담이 급증)
- 학생 간 자유 채팅, 담벼락 **댓글**, 익명 게시 (사이버 폭력 관리 부담 — 반응 이모지까지만)
- 성적·점수 표시 (1학년 자유학기제, 상/중/하만 부여)
- QR 코드 진입 (학생 태블릿에서 인식 불가)
- 노션·패들렛의 기능 전체 복제 (필요한 것만 옮긴다)
- **학생 브라우저의 Firestore 직접 접근** (명렬표가 학생 클라이언트로 내려가는 경로를 만들지 않는다)
- **학생이 수업 화면을 스스로 이동하는 것** (30명이 흩어지면 수업을 끌고 갈 수 없다)

---

## 10. 확인된 결함 (미해결)

2026-08-11 코드 리뷰에서 확인된 것. **아직 고치지 않았다.** 이 문서의 상태 표시(◐)는 대부분
여기에 근거한다.

### D1 — 무드미터 사분면 배치가 안내 문구와 반대 🔴

| | |
|---|---|
| 위치 | `src/components/mood-picker.tsx` (배치), `src/app/teacher/board/page.tsx` (공유 화면) |
| 증상 | 안내는 "위=기운 높음, 오른쪽=기분 좋음"인데 실제는 "가로=기운, 세로=기분(위가 불쾌)". 두 축이 뒤바뀜 |
| 영향 | **저장값은 정확하다** (감정어 버튼을 누르므로). 학생이 안내대로 위치를 찾으면 엉뚱한 사분면에 도착해 혼란스럽고, 색으로 축을 읽는 설계가 무너진다 |
| 고치는 법 | 배치 순서를 위 `[빨강][노랑]`, 아래 `[파랑][초록]`으로. 공유 화면도 같이 |
| 급한가 | 데이터 손실은 없으므로 소급 문제는 아니다. 다만 학생이 매 차시 겪는 혼란이라 이르게 고칠수록 좋다 |

### D2 — Firestore 읽기량이 무료 한도를 위협 🔴

| | |
|---|---|
| 위치 | `src/app/api/teacher/dashboard/route.ts`, `src/app/teacher/dashboard/page.tsx` |
| 증상 | 5초 폴링마다 출석·감정·성찰·명렬표 전체 재조회 → 115~116건/회, 30분 수업 1회에 최대 약 41,000건 (대시보드 단독·모든 응답이 채워진 상태 기준) |
| 영향 | 하루 2~3차시면 Spark 한도(50,000건/일) 초과. **수업 도중 읽기가 거부될 수 있다** |
| 고치는 법 | **조합이 필요하다.** ① 폴링 간격 5초→15초 (수업당 약 14,000건까지 내려가지만, 하루 3차시 + 공유 화면이면 여전히 한도에 근접) ② 명렬표는 최초 1회만 (수업당 약 10,000건 절감) ③ 집계 캐시 — 단, 대시보드는 개인별 행을 돌려주므로 집계만으로는 원문 조회가 없어지지 않는다 |
| 확인 | 고친 뒤 **실제 사용량을 측정**해야 한다. 추정만으로 안전하다고 판단하지 않는다 |
| 급한가 | **그렇다.** 수업이 멈추는 종류의 문제다 |

### D3 — 교시 시각표가 2026-08-21까지만 등록됨 🟡

| | |
|---|---|
| 위치 | `src/lib/timetable.ts` |
| 증상 | 단축 시간표 한 구간만 있다. 그 밖의 날짜는 `isPeriodOver()`가 항상 `false` |
| 영향 | **8/22부터 코드 자동 만료가 조용히 꺼진다.** 오류도 경고도 없다 |
| 고치는 법 | 정상 시간표 구간을 추가. 시각표가 없을 때 교사 화면에 경고를 띄우는 것도 검토 |
| 급한가 | 8/22 이전까지 |

### D4 — 종료하지 않은 세션이 다음 교시에도 선택됨 🟡

| | |
|---|---|
| 위치 | `src/lib/pick-session.ts`, `src/lib/db.ts` (`isSessionClosed`) |
| 증상 | 자동 만료는 `status`를 바꾸지 않으므로, 종료를 안 누른 세션이 계속 `active`로 남아 최우선 선택된다 |
| 영향 | 3교시에 앞 반 세션이 선택된다. 공유 화면(`/teacher/board`)에는 **앞 반의 익명 감정 집계**가, 대시보드에는 **앞 반의 개인별 감정·이유·성찰**이 뜬다 (4.3이 막으려던 바로 그것) |
| 고치는 법 | 후보에서 이미 닫힌 세션을 제외하거나, 조회 시 자동으로 `ended` 처리 |
| 우회 | 교사가 매 수업 끝에 **수업 종료**를 누르면 발생하지 않는다 |

### 문서·안내 불일치

| 항목 | 내용 | 참조 |
|---|---|---|
| AI 고지 | 학생 화면은 "AI 도구를 사용합니다"라고 안내하는데 AI 연동은 없음 | 5.3 |
| README 기능 설명 | "한 줄 성찰", "슬라이드 URL 등록" 등 바뀌기 전 구조를 그대로 설명 중 | — |
| README 환경변수명 | `CLIENT_EMAIL` / `PRIVATE_KEY`로 적혀 있으나 실제는 `FIREBASE_CLIENT_EMAIL` / `FIREBASE_PRIVATE_KEY` (`.env.example` 기준) | — |
