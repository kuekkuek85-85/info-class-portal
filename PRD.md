# 수업 포털 웹앱 PRD

장평중학교 1학년 정보과 · 2026학년도 2학기
작성 기준일: 2026-08-10 / 1차시 운영일: 2026-08-11

---

## 1. 배경과 목표

2학기부터 모든 수업 슬라이드를 웹앱으로 제작한다. 학생이 매번 다른 링크를 찾아다니지 않도록, **하나의 대문(포털)에 들어오면 오늘 자기 반 수업으로 바로 이어지는** 구조를 만든다.

포털은 네 가지를 겸한다.

1. **수업 진입점** — 오늘 수업 슬라이드로 연결
2. **활동 기록 누적** — 감정 체크, 한 줄 성찰이 학기 내내 쌓임
3. **출석 확인** — 접속 기록으로 대체
4. **학생 산출물 공유** — 결과물을 올리고 서로 보는 담벼락

### 대체 대상

이 앱은 지금까지 나눠 쓰던 도구들을 하나로 합치는 것을 목표로 한다.

| 기존 도구 | 대체하는 기능 | 도입 시기 |
|---|---|---|
| 노션 | 대문, 수업 자료·링크 아카이브, 지난 수업 다시 보기 | 8~9월 |
| 패들렛 | 학생 산출물 게시·공유 담벼락 | 9월 이후 |
| (신규) | 수업 슬라이드 구동 | 8월~ |
| (신규) | 교사용 대시보드 — 출석·감정·성찰·진행 상황 | 8월~ |

도구를 하나로 모으는 실질적 이유: 30분 수업에서 도구를 전환할 때마다 접속·설명에 2~3분이 사라진다. 로그인 체계가 하나면 그 손실이 없다.

### 설계 제약 (중요)

| 제약 | 영향 |
|---|---|
| 실질 수업 시간 **30분** (40분 단축수업 − 태블릿 수령 2분 · 반납 5분, 부팅 지연 포함) | 진입 동선은 **1분 내** 완료되어야 함 |
| 학생 태블릿 **QR 인식 불가** | 링크 직접 배포 |
| 학생 28명 **동시 접속** (한 번에 한 반) | 초기 로딩 최소화 |
| 중학교 1학년 | 입력은 숫자 위주, 화면당 할 일 하나 |

### 규모

- 1학년 **1~4반**, 반당 약 28명, 총 약 **112명**
- 반당 주 3차시 × 4반 = **주 12차시**, 주 5일 기준 하루 2~3차시
- 하루 쓰기 약 250건 / 읽기 약 1,500건 → **Firestore 무료 한도의 1~3%**

### 인프라 결정

- **Firebase(Firestore) + Vercel**, Spark(무료) 플랜으로 시작
- 리전 **asia-northeast3(서울)** — 생성 후 변경 불가하므로 최초에 지정
- Google Cloud **예산 알림** 설정 (이상 징후 조기 감지)
- 무료 티어에 자동 백업이 없으므로 **주 1회 CSV 내보내기**를 운영 습관으로

---

## 2. 사용자

- **학생** — 1학년. 태블릿 브라우저로 접속. 계정·비밀번호 없음.
- **교사** — 수업 진행자. 대시보드 열람, 명렬표·수업·코드 관리.

---

## 3. 핵심 동선

### 3.1 진입 · 인증 (P0)

순서가 중요하다. **수업 코드를 먼저 받는다.**

```
① 수업 코드 (숫자 2자리)
     ↓  코드 유효성 + 오늘 그 교시 수업 매칭
② 학번 (숫자 5자리, 예: 10709 = 1학년 7반 9번)
     ↓  명렬표 대조 + 코드에 묶인 반과 학번 앞 3자리 일치 검사
③ 확인 팝업 "1학년 7반 9번 ○○○ 학생 맞나요?"
     [네, 맞아요]  [아니에요 → ②로 복귀]
     ↓
④ 오늘 수업 화면
```

**코드를 먼저 받는 이유**: 학번이 규칙적(`10709`)이라 학번을 먼저 받으면 아무 숫자나 넣어 이름을 조회할 수 있다. 코드가 앞에 있으면 그 교실에 있는 학생만 이름 확인 화면에 도달한다.

**반 불일치 차단**: 수업 코드는 `(날짜, 교시, 반)`에 묶인다. 3교시 코드가 1학년 7반 수업이면 학번 앞 3자리가 `107`이 아닌 학생은 통과하지 못한다. 다른 반 코드를 알아내도 소용없다.

**같은 반 친구 학번 도용**: 앱에서 막지 않는다. 교사가 수업 중 출석 확인으로 통제한다. (교사 대시보드에 접속자 명단을 실시간 표시)

**명렬표에 없는 학생 (전입·오류)**
그 반 + 90번대 임시 번호로 진입 (예: `10791`). 임시 번호는 이름 없이 통과시키고, 교사 화면에서 나중에 실제 학번과 연결한다. 수업 흐름을 끊지 않는 것이 목적.

**세션 유지**: 한 차시 동안 재입력이 없도록 브라우저에 세션 유지. 만료는 당일 자정.

### 3.2 수업 슬라이드 열기 (P1)

- 진입 직후 **오늘 그 교시 수업이 자동으로 열린다.** 날짜·교시를 고르는 단계를 두지 않는다 (30명 × 클릭 1회 ≒ 1분).
- 별도 메뉴에 **지난 수업 다시 보기** — 날짜·교시 목록에서 선택.
- 슬라이드는 외부 웹앱으로 만들어지므로, 포털은 **수업별 슬라이드 URL을 보관하고 임베드 또는 이동**시킨다.
- 유튜브 등 외부 영상은 **슬라이드 안에 심어둔다.** 새 창으로 유튜브를 열면 학생 화면이 각자 흩어진다.

### 3.3 감정 체크 (P1)

매 차시 도입에 수행. 사회정서교육 연계.

**학생 화면**
- 오늘 기분 선택 (아이콘/색)
- 왜 그런지 한 줄 입력
- 입력칸 옆 **상설 안내 문구** (아래 5.3 참조)

**공개 범위 — 확정**

| | 학생에게 | 교사에게 |
|---|---|---|
| 감정(아이콘·색) | 익명 집계로 공개 | 개인별 열람 |
| 이유 한 줄 | **비공개** | 열람 (+ AI 요약, 6.5 참조) |
| 자기 기록 | 본인 것만 열람 | — |

**교사 대시보드 시각화** — 같은 데이터를 여러 형태로 전환해서 교실 앞 화면에 띄운다.
- 무드미터 (2축 사분면)
- 나침반형
- 무지개/분포형

시각화는 **이름과 이유가 절대 나타나지 않는 뷰**로 분리한다. 개별 응답 열람은 별도 화면. 전체 공유 화면에 실수로 개인 응답이 뜨는 사고를 구조적으로 막는다.

**출석과의 분리**: 출석은 ③ 인증 완료 시각(접속 로그)으로 확정한다. 감정 응답 여부는 출석 요건이 아니다. 감정을 출석 근거로 삼으면 학생이 아무거나 찍고 넘어가는 것으로 학습한다.

### 3.4 한 줄 성찰 (P0)

매 차시 마지막 활동. 오늘 배운 것·후기를 한 줄 남긴다.

- 차시마다 **교사가 지정한 질문**을 띄울 수 있어야 한다 (기본 질문 + 차시별 커스텀).
- 학생은 자기 글의 **누적 기록**을 볼 수 있다 (한 학기치 성장 기록).
- 다른 학생 글의 공개 여부는 **차시별 교사 설정**으로 켜고 끈다. 기본값은 비공개.
- 30분 수업의 마지막 활동이므로 **미완성 상태로 종료되어도 저장**되어야 한다 (입력 중 자동 임시저장).

### 3.5 산출물 담벼락 (P2 — 9월 이후)

패들렛을 대체하는 부분. 학생이 결과물을 올리고 서로 본다.

**올릴 수 있는 것**: 텍스트 · 이미지 · 링크. 차시별 담벼락 단위.

**공개 범위**: 같은 반, 같은 차시 안에서만. 다른 반 담벼락은 보이지 않는다.

**상호작용 — 최소한만 연다**

| 기능 | 채택 | 이유 |
|---|---|---|
| 반응(이모지 1~2종) | ○ | 참여감은 주되 서열화는 피함 |
| 댓글 | **×** | 사이버 폭력 관리 부담이 급증 |
| 익명 게시 | **×** | 실명(학번+이름) 표시가 안전장치 |
| 교사 즉시 숨김 | ○ | 부적절 게시물 1클릭 처리 |

패들렛에서 흔히 생기는 문제(장난 게시물, 서로 저격, 익명 뒤에 숨기)를 **기능을 안 만드는 방식으로** 막는다. 9월에 사이버 폭력을 가르치는 수업에서, 플랫폼 자체가 그 사고를 만들면 곤란하다.

**이미지 업로드 주의 — Blaze 필요**

Firebase Cloud Storage는 2026년 2월부터 Spark 플랜에서 사용할 수 없다. 이미지 업로드를 넣는 순간 **Blaze 전환이 필요**하다.
- 사용량 자체는 무료 한도 안에 들어갈 가능성이 높다
- 다만 예산 알림은 반드시 설정한다
- 이미지 없이 텍스트·링크만으로 시작하면 Spark 유지 가능

**용량 관리**: 112명 × 이미지가 학기 내내 쌓이면 무시할 수 없다. 업로드 시 리사이즈, 학기말 일괄 삭제 정책 적용.

---

## 4. 교사 화면

| 기능 | 우선순위 | 비고 |
|---|---|---|
| 오늘 접속자 실시간 명단 | P0 | 출석 확인 겸용 |
| 감정 대시보드 (공유용, 익명) | P1 | 시각화 형태 전환 |
| 감정 개별 응답 열람 | P1 | 공유용과 화면 분리 |
| 성찰 글 모아보기 | P0 | 차시별 / 학생별 |
| 명렬표 관리 | P0 | CSV 업로드(112행), 임시번호 연결 |
| 차시 등록 (`lesson_plans`) | P0 | 4반 공용, 슬라이드 URL·성찰 질문 |
| 시간표 입력 → 세션 자동 생성 | P0 | 반별 요일·교시 등록 시 학기 전체 생성 |
| 반 전환 (1~4반 탭) | P0 | 같은 날 여러 반 데이터 분리 |
| 수업 후 교사 메모 | P1 | 다음 반 개선 루프용 |
| 수업 코드 발급·조회 | P0 | 자동 생성 + 교사 확인 |
| 데이터 내보내기 | P2 | 학기말 기록 정리용 |
| 데이터 일괄 삭제 | P1 | 5.2 참조 |

**교사 인증**: 학생 동선과 완전히 분리된 경로. 교사만 아는 URL + 비밀번호 정도로 시작하되, 학생이 우연히 도달할 수 없어야 한다.

---

## 5. 데이터

### 5.1 모델 (초안)

```
students        학번(PK) · 이름 · 반 · 임시여부

lesson_plans    차시번호 · 제목 · 슬라이드URL · 성찰질문 · 감정체크여부
                  ↑ 수업 내용. 반과 무관. 한 번만 등록해 4반에 공용

class_sessions  lesson_plan_id · 반 · 날짜 · 교시 · 수업코드
                · 스냅샷(슬라이드URL · 성찰질문) · 교사메모
                  ↑ 언제 어느 반에서 하는지. 시간표에서 자동 생성

attendance      학번 · session_id · 접속시각          ← 출석
mood_entries    학번 · session_id · 감정 · 이유 · 시각
reflections     학번 · session_id · 내용 · 시각
```

**`lesson_plans` / `class_sessions` 분리 이유**

4반이 같은 진도를 나가므로, 슬라이드와 성찰 질문을 4번 등록하는 구조는 피한다. 한 번 등록하면 4반 세션에 자동 적용된다.

**스냅샷이 필요한 이유 (중요)**

교사는 1반 수업을 해보고 부족한 부분을 **다음 반 수업 전에 고쳐서** 진행한다. 이때 `lesson_plans`만 수정하면 이미 끝난 1반 기록이 소급 변경되어, "1반은 어떤 질문에 답한 것인가"를 알 수 없게 된다.

→ 세션 시작 시점에 슬라이드URL·성찰질문을 **세션에 복사(스냅샷)**한다.
- 수정은 **아직 시작하지 않은 세션에만** 반영된다
- 끝난 세션은 그날 실제로 쓴 내용을 그대로 보존한다
- 성찰 답변을 나중에 읽을 때 질문과 짝이 맞는다

**교사 메모**

세션마다 수업 직후 한 줄 회고를 남길 수 있게 한다 ("영상 2분 넘어가니 늘어짐", "성찰 질문 2번 어려워함"). 다음 반 수업 전에 이 메모를 확인하는 것이 개선 루프의 출발점이다.

**원칙: 이름은 `students`에만 둔다.** 활동 기록에는 학번만 저장하고, 화면에 보여줄 때만 조인한다. 기록 데이터가 유출되어도 숫자만 남는다.

### 5.2 보관 · 삭제 정책

처음부터 넣는다. 나중에 붙이기 훨씬 어렵다.

| 데이터 | 보관 | 삭제 |
|---|---|---|
| 명렬표(이름) | 학기 중 | 학기 종료 시 삭제 |
| 감정 — 이유 한 줄 | 최소 기간 | **월 단위 자동 삭제** 권장 |
| 감정 — 아이콘·색 | 학기 중 | 학기 종료 시 삭제 |
| 성찰 글 | 학기 중 | 학기 종료 시 삭제 (필요분은 사전 내보내기) |
| 접속 로그 | 학기 중 | 학기 종료 시 삭제 |

교사 화면에 **일괄 삭제 버튼**과 **마지막 삭제 일자 표시**를 둔다.

### 5.3 안내 문구 (상설 노출)

감정 입력칸 옆에 항상 표시.

> 기분은 다 같이 보고, 적은 이유는 선생님이 봅니다.
> 내용을 정리할 때 AI 도구를 사용합니다.
> 힘든 일이 있으면 담임 선생님이나 상담 선생님께 이야기해 주세요.

두 번째 줄은 감정 데이터를 AI로 분석하기로 한 결정에 따른 것이다. 학생에게 "선생님만 봅니다"라고 안내하고 실제로는 외부 API를 거치면 약속이 깨진다. 9월에 개인정보 단원을 가르치는 수업에서 이 불일치는 특히 치명적이다.

세 번째 줄이 중요하다. **이 앱은 상담 창구가 아니다.** 매일 여러 반의 감정을 받으면 걱정되는 문장은 언젠가 반드시 들어온다.

### 5.4 교사 확인 루틴

PRD에 명시해 둔다: 교사는 **매 수업 종료 후 그날 감정 응답을 확인한다.** 받아두고 보지 않는 상태가 가장 위험하다. 교사 화면에 **미확인 응답 표시**를 두어 확인 여부를 추적한다.

---

## 6. 1차시 (8/11) 범위

30분, 첫 사용. **동작 보장이 최우선.**

| 구분 | 기능 |
|---|---|
| **반드시 동작** | 진입·인증 / 한 줄 성찰 게시글 |
| 되면 좋음 | 감정 체크 |
| 없어도 됨 | 학생 태블릿에서 슬라이드 열기 (교사 화면 투사로 대체 가능) |
| 이번엔 제외 | 지난 수업 다시 보기, 데이터 내보내기, 시각화 전환 |

### 1차시 흐름 (30분)

| 시간 | 내용 |
|---|---|
| 5분 | 포털 접속 · 인증 |
| 4분 | 도입 — 영상 (0:00~2:08, 7:25~8:46) |
| 5분 | 단원 지도 |
| 8분 | 수행평가 안내 |
| 3분 | 규칙 |
| 5분 | 한 줄 성찰 남기기 |

### 성찰 질문 (1차시)

1. 영상에서 AI가 아직 못 한다고 한 것 중 하나를 고르고, 왜 어려울지 내 생각 쓰기
2. 완전히 대체될 직업은 7% 미만이라는데, 그럼 나머지는 어떻게 되는 걸까?
3. "AI와 함께 일하는 능력"이란 구체적으로 뭘까?

### 실패 대비

- 포털이 열리지 않을 경우의 **대체 경로**를 준비한다 (성찰은 종이로).
- 첫날 "선생님 안 돼요"가 5분 이상 이어지면 학기 내내 인상이 남는다.
- **반납 5분 알람**을 고정한다.

---

## 6.5 AI 기능 (Gemini API)

차시에 따라 AI 기능을 붙일 수 있도록 준비한다. 1차시 범위에는 포함하지 않는다.

### 필수 원칙

**① API 키를 클라이언트에 두지 않는다**
학생 태블릿 브라우저에 키가 들어가면 그대로 노출된다. 반드시 **서버 경유** 호출.
- Firebase Cloud Functions는 Spark 플랜에서 사실상 쓸 수 없다 (Cloud Run·Artifact Registry 의존)
- 이미 Vercel을 쓰고 있으므로 **Vercel Serverless Function**을 프록시로 둔다
- 키는 Vercel 환경변수에 보관

**② 감정 데이터를 AI에 보낼 경우 — 유료 티어 필수**
감정 체크의 "왜 그런지 한 줄"을 AI로 분석·요약한다. 이 경우 다음이 전제 조건이다.

- **과금이 활성화된 Gemini API 키(유료 티어)만 사용한다.** 무료 티어는 제출 내용이 Google 제품 개선에 사용될 수 있고 사람이 검토할 수 있다. 중학생 정서 정보에는 부적합하다. 유료 티어는 학습에 사용되지 않는다.
- **비식별 전송**: 학번·이름을 제거하고 텍스트만 보낸다. 개인 매핑이 필요하면 요청 내 임시 인덱스(1, 2, 3…)만 사용한다.
- **배치 전송**: 개별 응답을 실시간으로 보내지 않고, 반·차시 단위로 묶어 보낸다. 비용도 절반이 된다.
- **용도 제한**: 경향 요약·분류까지만. **개별 학생에 대한 진단·라벨링은 금지**한다("이 학생은 우울함" 같은 출력). AI는 교사 판단의 보조이지 판정자가 아니다.
- **위기 신호**: AI로 1차 플래그를 띄우더라도 **반드시 교사가 원문을 확인**한다. AI 판정만으로 조치하지 않고, AI가 놓칠 수 있다는 전제로 교사 확인 루틴(5.4)은 그대로 유지한다.

**학생 안내 문구를 이에 맞춰 수정한다** (5.3 참조). "선생님만 봅니다"는 더 이상 정확하지 않다.

**학교 절차 확인**: 학생 정서 정보를 외부 API로 전송하는 것이 학교 개인정보 처리 방침과 보호자 안내 범위에 포함되는지 담당자와 확인한다.

**③ 학생 입력은 템플릿 안에서만**
프롬프트 전문을 학생이 자유 작성하게 두지 않는다. 서버에서 고정 프롬프트 + 학생 입력값 조합. 남용과 이탈을 구조적으로 막는다.

**④ 호출 한도**
학생 1인 1차시당 호출 횟수 상한을 서버에서 강제한다 (예: 5회). 112명 × 무제한은 비용·속도 양쪽에서 위험하다.

**⑤ 사용 로그를 남긴다**
학번 · session_id · 요청 시각 · 사용한 기능. 수행평가1이 AI 활용을 허용하고 **출처 밝히기 태도**를 평가하므로, 이 로그가 평가 근거가 된다. 학생에게도 "AI 사용 기록이 남는다"고 미리 안내한다.

### 기능 후보 (교사용부터)

| 대상 | 기능 | 우선순위 |
|---|---|---|
| 교사 | 성찰 글 112건 요약 · 키워드 추출 | 높음 |
| 교사 | 반별 응답 경향 정리 | 중간 |
| 학생 | 리포트 초안에 대한 피드백 (수행평가1) | 중간 |
| 학생 | 엔트리 코드 설명 (코딩 단원) | 낮음 |

**교사용을 먼저 붙이는 것이 안전하다.** 사용자가 1명이라 비용·남용 위험이 없고, 성찰 글 112건을 매주 눈으로 읽는 부담을 바로 덜어준다.

---

## 7. 학기 전체 로드맵

| 시기 | 추가 기능 |
|---|---|
| 8월 | 진입·인증, 성찰, 감정 체크 |
| 9월 | 감정 시각화 전환, 지난 수업 다시 보기, 수행평가 산출물 제출 |
| 10~11월 | 피지컬 컴퓨팅 프로젝트 기록 (과정 관찰용 로그) |
| 12월 | 데이터 내보내기, 학기말 일괄 삭제 |

수행평가와의 연계
- **수행평가1 디지털 시민 리포트** (8~9월, 서술·논술·포트폴리오) — 성찰 글 누적이 포트폴리오 근거가 된다. 출처 밝히기 태도를 평가하므로, 제출 양식에 **출처 입력란**을 두는 것을 검토.
- **수행평가2 피지컬 컴퓨팅 프로젝트** (10~11월, 실험실습) — 문제 발견부터 오류 수정까지의 **과정**을 평가하므로, 학생이 진행 상황을 차시별로 남기는 기록 기능이 필요.

---

## 8. 열린 결정 사항

개발 착수 전에 정해야 할 것들.

1. **한 반의 학기 총 차시 수** — 시간표 자동 생성 범위를 정한다. 반당 24차시면 세션 96개, 반당 50차시면 약 200개.
2. **감정 선택지 구성** — 몇 개, 어떤 축(무드미터라면 쾌-불쾌 × 활성-비활성 4사분면).
3. **성찰 글 공개 기본값** — 비공개로 시작하되, 어느 시점에 공개형을 열지.
4. **공동 지도 교사** — 교사 계정이 둘 이상 필요한지, 반을 나눠 보는지.
5. **교사 인증 방식** — 초기 비밀번호 수준에서 어디까지 강화할지.
6. **학교망 접근 확인** — Firebase·Vercel 도메인이 학생 태블릿에서 열리는지 (기존 교실 웹앱 운영 이력으로 검증되었다면 생략).

### 확정된 사항

- **수업 코드**: 자동 생성 2자리. `(날짜, 교시, 반)`에 묶이고 교시 종료 시 만료. 같은 날 중복 발급 금지, 다음 날 재사용 가능. 하루 최대 3개 동시 유효.
- **인프라**: Firebase(Firestore) + Vercel, Spark 무료 플랜, 서울 리전.

---

## 9. 하지 않을 것

- 학생 비밀번호 자체 저장 (개인정보 처리 부담이 급증)
- 학생 간 자유 채팅, 담벼락 **댓글**, 익명 게시 (사이버 폭력 관리 부담 — 반응 이모지까지만 허용)
- 성적·점수 표시 (1학년 자유학기제, 상/중/하만 부여)
- QR 코드 진입 (학생 태블릿에서 인식 불가)
- 노션·패들렛의 기능 전체 복제 (필요한 것만 옮긴다)
